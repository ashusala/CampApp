<!DOCTYPE html>
<html>
    <head>
        <title>YelpCamp</title>
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
        <link rel="stylesheet" href="/stylesheets/main.css">
    </head>
    <body>
        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <div class = "container-fluid">
                <a class="navbar-brand" href="/">YelpCamp</a>
    
                <div class = "collapse navbar-collapse">
                    <ul class = "ml-auto navbar-nav">
                        <% if(!currentUser){ %>
                            <li class="active"><a class="nav-link" href="/login">Login</a></li>
                            <li class="active"><a class="nav-link" href="/register">Sign Up</a></li>
                        <% } else{ %>
                            <li class="active"><a class="nav-link" href="#">Hi <%= currentUser.username %></a></li>
                            <li class="active"><a class="nav-link" href="/logout">Log Out</a></li>
                        <% } %>
                    </ul>
                </div>
            </div>
        </nav>
        
        <div class = "container">
            <% if(error && error.length>0){ %>
                <div class="alert alert-danger" role="alert">
                    <%= error %>
                </div>
            <% } %>
            <% if(success && success.length>0){ %>
                <div class="alert alert-success" role="alert">
                    <%= success %>
                </div>
            <% } %>
        </div>